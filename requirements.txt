# DeepReality — Dependencies
# pip install -r Requirements.txt

# Layer 1 — Preprocessing
Pillow>=10.0.0          # PIN-A1, PIN-A3: image loading, EXIF extraction, JPEG re-encoding
c2pa-python>=0.28.0     # PIN-A2: C2PA Content Credentials parsing and validation
opencv-python>=4.8.0    # PIN-A3, PIN-A4: ELA heatmaps, image processing, face cropping
numpy>=1.24.0           # PIN-A3, PIN-A4: statistical analysis
pillow-heif>=0.16.0     # PIN-A3: iPhone HEIC/HEIF format support
mediapipe>=0.10.0       # PIN-A4: face detection (BlazeFace) and landmarks

# Layer 2 — Detection Core
torch>=2.0.0            # PIN-B1/B2/B3/B4: deep learning framework
transformers>=4.40.0    # PIN-B1/B2/B4: Hugging Face model loading
scipy>=1.11.0           # PIN-B3: DCT transform (scipy.fft.dctn)
PyWavelets>=1.5.0       # PIN-B3: DWT wavelet decomposition

# Layer 4 — Explainability
# PIN-D1 requires no external XAI package: Grad-CAM is implemented with
# PyTorch hooks and torch.autograd.grad, which keeps the pin compatible
# with current transformers releases. torch and opencv suffice.

# Layer 5 — LLM Reasoning Engine
requests>=2.31.0        # PIN-E1: OpenAI-compatible chat-completions client

# Layer 6 — Ensemble Fusion
xgboost>=2.0.0          # PIN-F1: gradient-boosted meta-learner
scikit-learn>=1.4.0     # PIN-F1: Platt calibration, evaluation metrics
# macOS note: xgboost requires the OpenMP runtime -> brew install libomp
